let name = "Divyanshu" //String
let age = 20 // number
let isLogin = true // boolean

// undefined => We declare the value but doesn't assigned the any value

// null => StandAlone Value

// objects
console.log(typeof(name)); // shows the datatype of the value
